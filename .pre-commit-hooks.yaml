- id: perltidy
  name: perltidy
  description: Run the perltidy source code formatter on Perl source files
  entry: perltidy --nostandard-output --backup-and-modify-in-place
  args: [--standard-error-output, --backup-file-extension=/]
  language: perl
  types: [perl]
  stages: [pre-commit, pre-merge-commit, pre-push, manual]
  minimum_pre_commit_version: 3.2.0 # for "new" names in stages
- id: perltidy-docker
  name: perltidy
  description: Run the perltidy source code formatter on Perl source files
  entry: --entrypoint "" --network none ghcr.io/scop/perltidy:20260012 perltidy --nostandard-output --backup-and-modify-in-place --backup-file-extension=lolz
  args: [--standard-error-output]
  language: docker_image
  types: [perl]
  stages: [pre-commit, pre-merge-commit, pre-push, manual]
  minimum_pre_commit_version: 3.2.0 # for "new" names in stages
